Role: UX Interaction Specialist & Haptics Engineer

Task:
Inject "Visceral Interactions" into the React application to enhance the "Cyber-Mysticism" vibe.

1.  **"Soul Extraction" Submit Button:**
    -   Replace the final "View Result" button with a `LongPressTrigger`.
    -   **Interaction:** User must hold for 3000ms.
    -   **Visuals:**
        -   SVG progress circle fills up (red/orange).
        -   Screen shaking effect (intensity increases with time).
        -   Text scrambles: "Extracting Soul...", "Syncing Trauma...", "Uploading..."
    -   **Haptics:** Progressive vibration pattern (heartbeat accelerating).

2.  **"Judgmental" Selection Effects:**
    -   On selecting an option, trigger a specialized visual effect based on the option's weight:
        -   High `scheming`: Darken the screen vignette momentarily.
        -   High `mental_state` (negative): Apply a CSS `hue-rotate` glitch effect for 200ms.
        -   Neutral: Standard typewriter press effect.

3.  **"Idle Mockery" System:**
    -   If user idles on a question for > 6 seconds:
    -   Show a floating toast (Tooltip): "Hesitation detects guilt." (犹豫即是心虚)
    -   Blur the unselected options slightly to force focus.

4.  **Haptic Soundscape (Mobile Only):**
    -   Implement a `HapticUtils.js` helper.
    -   `tap`: vibrate(10) (Question switch)
    -   `clack`: vibrate(25) (Option select)
    -   `thud`: vibrate(300) (Result Stamp landing)
    -   **Note:** Check `navigator.vibrate` support before calling.

5.  **Tech Stack:**
    -   Use `framer-motion` for the shake/scramble animations.
    -   Use `use-long-press` hook for the submit button.

Please update `App.jsx` and create the `LongPressTrigger` component.